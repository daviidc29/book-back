name: Build and deploy JAR app to Azure Web App - book

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read 

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java version
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'microsoft'

      - name: Build with Maven
        run: mvn clean install

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: java-app
          path: '${{ github.workspace }}/target/*.jar'

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write 
      contents: read 
    
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: java-app
        
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_6393C8C37502402CAA891170ABB7A703 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_A01150A498344CC09D9E4489CCFE36E0 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_40A441193CE5420B983D3EF0254F3113 }}

      # CORRECCIÓN: Configurar la variable de entorno directamente en Azure
      - name: Set App Settings for Web App
        uses: azure/appservice-settings@v1
        with:
          app-name: 'book'
          app-settings-json: '[{ "name": "N8N_WEBHOOK_URL", "value": "${{ secrets.N8N_WEBHOOK_URL }}", "slotSetting": false }]'

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'book'
          slot-name: 'Production'
          package: '*.jar'
